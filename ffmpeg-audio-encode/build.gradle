apply from: "$rootDir/module.gradle"

android {
    buildTypes {
        debug {
            jniDebuggable true
        }
        release {
            minifyEnabled true
            shrinkResources true
        }
    }
}