apply from: "$rootDir/module.gradle"

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation project(':common-asserts')
}
android {
    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
        }
    }
}
